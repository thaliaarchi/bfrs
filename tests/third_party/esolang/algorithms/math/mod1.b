[Modulo

If we do not need to compute the quotient as well, the following approach is
shorter than the divmod algorithm.

# 0 >n 0 d 0 0 0]

[>+>->+<[>]>[<+>-]<<[<]>-]

[# 0 >0 n d-n%d n%d 0 0

As an additional advantage, this algorithm works even if the divisor is 1.]
