[x´ = x == y (wrapping)

If you don't need to preserve x or y, the following does the task without
requiring any temporary blocks. Returns 0 (false) or 1 (true).

Layout: x y]

x[-y>-x<]+y>[x<-y>[-]]

[It is similar to z = x xnor y.]
